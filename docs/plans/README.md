# docs/plans — retired 2026-08-02

This directory held 69 documents and 20 390 lines. It is empty of plans now, and
that is the point.

- **The living surface is [`docs/idea/`](../idea/00-index.md)** — ten documents,
  hard ceiling. An eleventh idea means one absorbs it or one is finished.
- **The long form is [`docs/archive/`](../archive/)** — every plan that was here,
  preserved. Nine had shipped; 38 (`v07-overnight/`) never ran at all; the rest
  are the detail behind the ten.
- **State — dates, statuses, nesting — lives in the `nOS Roadmap` DataTable**
  (`tools/roadmap-seed.py`), not in prose, because prose drifts and a table you
  read every day does not.

**Do not add a plan here.** Extend one of the ten, or make the case that it needs
an eleventh and retire one.
