## NOS Security Monitor — 2026-04-10 (Scan Cycle 10)

### Fáze 1: Advisory Check
- **Batch:** openwebui, nginx, portainer, gitea, mariadb
- **Nové CVE:** 5 findings (0 CRITICAL, 2 HIGH, 1 MEDIUM, 2 already mitigated)

| ID | Component | Finding | Severity | Status |
|----|-----------|---------|----------|--------|
| REM-063 | nginx | CVE-2026-27654 + CVE-2026-27784 + CVE-2026-32647 + CVE-2026-27651 (DAV module buffer overflow, MP4 module buffer overflow/code exec, NULL ptr deref) | HIGH | Pending — `brew upgrade nginx` to >= 1.29.7 |
| REM-064 | openwebui | CVE-2026-0765 + CVE-2026-0766 (ZDI-26-031/032: command injection RCE via install_frontmatter_requirements + load_tool_module_by_id, CVSS 8.8) | HIGH | Pending — admin-only, no confirmed vendor patch |
| REM-065 | openwebui | CVE-2026-28786 (path traversal info disclosure in audio transcriptions, CVSS 4.3, fixed in 0.8.6) | MEDIUM | Pending — our 0.6.35 vulnerable |
| REM-066 | gitea | CVE-2026-20897 + CVE-2026-20750 + CVE-2026-20888 (LFS lock auth bypass, org project bypass, auto-merge logic flaw) | HIGH | Resolved — mitigated in our 1.25.5 |
| REM-067 | mariadb | CVE-2026-35549 (memory allocation excessive size, CVSS 6.5) | MEDIUM | Resolved — mitigated in our 11.4.10 |

**Portainer:** No new CVEs found. CVE-2026-22557 is UniFi, not Portainer.

### Fáze 2: Pentest
- **Target:** Redis 7.4.6 / RESP protocol fuzzing + Lua sandbox review
- **Výsledek:** no_findings
- **Coverage:** 1/3 areas (33%) for Redis; 6/28 total areas (21%)
- **Soubory:** 14 files reviewed across src/networking.c, src/util.c, src/eval.c, src/script_lua.c, deps/lua/src/loslib.c

**Key observations:**
- RESP parser: multibulk length capped INT_MAX (unauth: 10 elements), bulk length validated with proto_max_bulk_len (512MB, unauth: 16KB), query buffer 1GB/1MB limits, argv growth INT_MAX/2 overflow-safe
- Lua sandbox: os module restricted to os.clock() only (custom sandbox_syslib), debug module removed after error handler init, package module not loaded, globals recursively readonly-locked, dangerous builtins (dofile, loadfile, setfenv, getfenv) removed
- `__redis__err__handler` captures debug module as upvalue `dbg` — not exploitable (no debug.getupvalue available to user scripts)

### Fáze 2b: Patches
Žádné nové patche (no findings in this cycle).

### Housekeeping
- **versions.json sync:** OK — n8n (2.14.1), traefik (v3.6.12), all versions match default.config.yml
- **Resolved items:** 11/67 (under 50 threshold, no archiving needed)
- **Glasswing repos:** 5 repos cloned (authentik, n8n, open-webui, redis, rustfs)
- **Disk check:** Unable to run du (tool aliasing conflict) — manual check recommended

### Action Items

**CRITICAL:** žádné

**HIGH:**
- [ ] `brew upgrade nginx` — ensure >= 1.29.7 (CVE-2026-27654 DAV buffer overflow + CVE-2026-27784/32647 MP4 buffer overflow). Check if DAV/MP4 modules are enabled in nginx.conf.
- [ ] Open WebUI CVE-2026-0765/0766: Monitor upstream for patch. Restrict tool creation to trusted admins. Consider disabling custom tool upload if not needed.

**MEDIUM:**
- [ ] Open WebUI CVE-2026-28786: Path traversal info disclosure. Low risk (behind SSO, only leaks DATA_DIR path). Fix requires upgrade to 0.8.6 (major jump from 0.6.35).

**PENTEST:**
- Příští area: Redis ACL bypass via command aliasing (src/acl.c) — medium priority
- Alternatively: Traefik HTTP/2 request smuggling or Vaultwarden org permission model (both high priority, no cooldown)

### Příkazy

```bash
# Nginx upgrade (fixes REM-063)
brew upgrade nginx
nginx -v  # verify >= 1.29.7

# Check if DAV/MP4 modules are enabled
grep -E 'dav|mp4' /opt/homebrew/etc/nginx/nginx.conf

# Verify Open WebUI version
docker inspect --format='{{.Config.Image}}' $(docker ps -qf name=open-webui) 2>/dev/null
```
