# Authentik Aggregator Parity Report — D3 dry-run (2026-05-05)

> Generated by `tools/aggregator-dry-run.py`. Compares the central
> `authentik_oidc_apps` list (pre-Q2, in `default.config.yml`) against
> per-plugin `authentik:` blocks aggregated by the loader into
> `authentik-base.inputs.clients`. Run before each Phase 2 C1 attempt.

## Headline numbers

| Source | Entries |
|---|---|
| central `authentik_oidc_apps` | 34 |
| plugin `inputs.clients` | 35 |
| both sources | 33 |
| only central (C1 blocker) | **1** |
| only plugin (post-Q2 net coverage) | 2 |

## C1 blockers

### Slugs in central with NO matching plugin (1)

- **`spacetimedb`** (`type: proxy`) — Q3 candidate per
  `docs/track-q-residue-analysis.md`. No `spacetimedb-base/plugin.yml`
  yet. **Decision needed:** create a Q3 plugin stub now (just the
  `authentik:` block — full migration deferred), OR keep the central
  entry as residual until Q3 lands. Latter means C1 keeps a one-row
  `authentik_oidc_apps` list instead of deleting the variable
  outright; we can flag-gate that.

## Mode mismatches (4)

Central treats these as `oauth2` (native OIDC providers). Plugins
declare `forward_auth`/`proxy`. **Central is the source of runtime
truth** — each role wires native OIDC at runtime:

| Slug | Central | Plugin | Runtime evidence |
|---|---|---|---|
| `erpnext` | `oauth2` | `forward_auth` | `roles/pazny.erpnext/tasks/post.yml:139-198` creates Frappe `Social Login Key` doctype (native OIDC) |
| `homeassistant` | `oauth2` | `forward_auth` | `roles/pazny.homeassistant/tasks/main.yml` installs `auth_oidc` custom HA component |
| `jellyfin` | `oauth2` | `forward_auth` | `roles/pazny.jellyfin/templates/SSO-Auth.xml.j2` — Jellyfin SSO-Auth plugin config |
| `superset` | `oauth2` | `forward_auth` | `roles/pazny.superset/templates/superset_config.py.j2` — `OAUTH_PROVIDERS` block |

`CLAUDE.md`'s "IAM & SSO" → "Proxy auth" list is **stale** for these
4 services; the plugin scaffolds inherited that staleness. **Fix
direction:** flip plugin `mode: forward_auth` → `native_oidc` (or
remove the field; loader normalizes missing → native_oidc), then
update CLAUDE.md.

If C1 lands without fixing the plugins, Authentik would create
**proxy providers** for these 4 services, breaking their native
OIDC sign-in (existing operator sessions get 401 at the proxy
provider's external_host check).

## Net-new plugin coverage (2)

- `qdrant` — declared in `qdrant-base/plugin.yml` for forward-auth
  proxy. No central entry. Pure coverage win.
- `woodpecker` — declared in `woodpecker-base/plugin.yml`. Pure
  coverage win.

## Slugs aligned (no diff: 29)

bookstack, calibre-web, code-server, firefly, freescout, gitea,
gitlab, grafana, hedgedoc, infisical, influxdb, kiwix, mailpit,
metabase, miniflux, n8n, nextcloud, nodered, ntfy, onlyoffice,
open-webui, outline, paperclip, portainer, puter, uptime-kuma,
vaultwarden, wing, wordpress.

## Migration-ready criterion

Script exits **0** when `only_central` is empty. Today it exits **1**
because of `spacetimedb`. After the fix path below it should exit 0,
gating C1.

## D1 fix path (in order)

1. Fix 4 mode mismatches in plugins (`erpnext-base`, `homeassistant-base`,
   `jellyfin-base`, `superset-base`): `provider_type: forward_auth` →
   `native_oidc`. Add `redirect_uris:` (list) where missing.
2. Decide spacetimedb path: scaffold `spacetimedb-base/plugin.yml`
   with proxy `authentik:` block, OR leave central residual.
3. Re-run `tools/aggregator-dry-run.py` — must exit 0.
4. Update `authentik-base/blueprints/10-oidc-apps.yaml.j2` to iterate
   over a merged source: `inputs.clients | default([])` first, then
   `authentik_oidc_apps | default([])` as fallback (no overlap because
   the dry-run guarantees disjoint coverage). Phase the migration:
   first commit feeds both sources (idempotent), second commit deletes
   `authentik_oidc_apps` from `default.config.yml` and switches
   blueprint to single-source.
5. Sync `CLAUDE.md` "IAM & SSO" lists with reality (move erpnext +
   homeassistant + jellyfin + superset out of "Proxy auth" → "Native
   OIDC").
