# Authentik Aggregator Parity Report — post-C1 dry-run (2026-05-23)

> Generated by `tools/aggregator-dry-run.py --verbose`. Compares the
> retired central `authentik_oidc_apps` list against per-plugin
> `authentik:` blocks aggregated by the loader into
> `authentik-base.inputs.clients`.

## Headline numbers

| Source | Entries |
|---|---:|
| central `authentik_oidc_apps` | 0 |
| plugin `inputs.clients` | 38 |
| both sources | 0 |
| only central (C1 blocker) | 0 |
| only plugin (post-Q2 coverage) | 38 |
| field diffs | 0 |

## Status

C1 is closed. `default.config.yml` now keeps `authentik_oidc_apps: []`
only as a backwards-compatible Tier-2 extension stub; all real Authentik
clients come from plugin manifests (plus Tier-2 app manifests via
`from: app_manifest`).

The previous blockers are resolved:

| Former blocker | Resolution |
|---|---|
| `spacetimedb` central-only row | `files/anatomy/plugins/spacetimedb-base/plugin.yml` now declares the forward-auth client |
| `erpnext` mode mismatch | `erpnext-base` declares `native_oidc` + redirect URI |
| `homeassistant` mode mismatch | `homeassistant-base` declares `native_oidc` + redirect URI |
| `jellyfin` mode mismatch | `jellyfin-base` declares `native_oidc` + redirect URI |
| `superset` mode mismatch | `superset-base` declares `native_oidc` + redirect URI |

## Plugin-owned slugs

bookstack, calibre-web, code-server, erpnext, firefly, freescout,
gitea, gitlab, grafana, hedgedoc, homeassistant, infisical, influxdb,
jellyfin, kiwix, mailpit, metabase, miniflux, n8n, nextcloud, nodered,
ntfy, onlyoffice, open-webui, openclaw, outline, paperclip, portainer,
puter, qdrant, snappymail, spacetimedb, superset, uptime-kuma,
vaultwarden, wing, woodpecker, wordpress.

## Gate

```bash
python3 tools/aggregator-dry-run.py --verbose
```

Expected result: exit 0 with `only central (NOT covered): 0` and
`field-diffs: 0`.
