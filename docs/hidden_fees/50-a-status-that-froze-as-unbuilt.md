# 50 — A status that froze the engine as unbuilt

**Found** 2026-09-02; **closed** 2026-09-03 (one line).

`docs/idea/11-agentic-loop.md` said "Status: design, not built" while
`looproutes.py`, `loopauth.py`, `bin/nos-loop` and 49 loop tests existed — and
the index's own one-liner already admitted the per-session loop runs. The
header now says what the index knew: partial — the engine runs per session;
cross-session carry and addressable judges are the open half. Same freeze
class as fee 07; recorded separately because it froze in the direction that
UNDERSELLS, which is rarer and just as costly (a later agent would re-propose
the engine).
