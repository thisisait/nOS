---
galaxy_info:
  role_name: traefik
  namespace: pazny
  author: Pázny
  description: Traefik reverse proxy in Docker compose override (devBoxNOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — Traefik bezi jako soucast infra stacku vedle
# Portainer/Authentik/Infisical. Docker-socket-proxy zustava v base infra
# compose (sdileny s Portainerem).
dependencies: []

collections: []
