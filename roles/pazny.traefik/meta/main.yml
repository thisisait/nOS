---
galaxy_info:
  role_name: traefik
  namespace: pazny
  author: Pázny
  description: Traefik reverse proxy in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Traefik runs as part of the infra stack alongside
# Portainer/Authentik/Infisical. Docker-socket-proxy stays in base infra
# compose (sdileny s Portainerem).
dependencies: []

collections: []
