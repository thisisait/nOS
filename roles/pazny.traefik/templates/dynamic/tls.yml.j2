# {{ ansible_managed }}
# Traefik FILE-PROVIDER TLS — cert pairs from {{ tls_cert_dir }}.
# Container path: /certs (bind-mount from host {{ traefik_certs_dir }}:ro).

tls:
  certificates:
{% if tenant_domain_is_local | default(true) | bool %}
    - certFile: /certs/local-dev.crt
      keyFile:  /certs/local-dev.key
{% else %}
    - certFile: /certs/acme/{{ _acme_zone }}.crt
      keyFile:  /certs/acme/{{ _acme_zone }}.key
{% endif %}

  stores:
    default:
      defaultCertificate:
{% if tenant_domain_is_local | default(true) | bool %}
        certFile: /certs/local-dev.crt
        keyFile:  /certs/local-dev.key
{% else %}
        certFile: /certs/acme/{{ _acme_zone }}.crt
        keyFile:  /certs/acme/{{ _acme_zone }}.key
{% endif %}
