---
# Handlers are placeholders — nos_state is itself idempotent and writes
# atomically, so there is nothing to "restart". Kept as an extension point
# for future use (e.g. nudging BoxAPI to refresh its cache).

- name: state_manager refresh cache
  ansible.builtin.debug:
    msg: "state_manager: handler fired (no-op placeholder)"
