---
galaxy_info:
  role_name: bone
  namespace: pazny
  author: Pázny
  description: Bone — local FastAPI (structure / state / dispatcher organ of nOS). Formerly BoxAPI.
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Python3 and Homebrew are installed outside the role.
# Source files/anatomy/bone/ and templates/bone-launchd.plist.j2 stay in playbook repo.
# Tasks use only ansible.builtin, no extra collections.
dependencies: []
