---
galaxy_info:
  role_name: woodpecker
  namespace: pazny
  author: Pázny
  description: Woodpecker CI in Docker compose override (nOS devops stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Woodpecker (server+agent) runs as part of
# devops stack. Requires a running Gitea + a manually created OAuth2 client
# (config via woodpecker_gitea_client / woodpecker_gitea_secret).
dependencies: []

collections: []
