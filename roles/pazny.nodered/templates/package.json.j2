{
  "_comment": "β1.B (2026-05-05) — pre-seeds /data/node_modules so Node-RED's entrypoint runs `npm install` on first boot and brings up the OIDC strategy without manual `Manage palette` work. Regenerated by pazny.nodered/tasks/main.yml; do not edit by hand.",
  "name": "node-red-project",
  "description": "nOS Node-RED runtime deps (managed by pazny.nodered)",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
{% if install_authentik | default(false) and (nodered_native_oidc_enabled | default(false)) %}
    "passport-openidconnect": "^{{ nodered_passport_openidconnect_version | default('0.1.2') }}"
{% endif %}
  }
}
