/**
 * Node-RED settings.js - rendered by the pazny.nodered role.
 *
 * adminAuth is INTENTIONALLY DISABLED - editor access is gated by the Authentik
 * forward_auth snippet in the nginx vhost (templates/nginx/sites-available/
 * nodered.conf). All requests flow through https://{{ nodered_domain }}
 * -> nginx -> Authentik check -> proxy_pass to 127.0.0.1:{{ nodered_port }}.
 *
 * DO NOT EDIT MANUALLY - regenerated from settings.js.j2.
 */
module.exports = {
    uiPort: process.env.PORT || 1880,
    mqttReconnectTime: 15000,
    serialReconnectTime: 15000,
    debugMaxLength: 1000,
    // NO adminAuth -> gated by Authentik forward_auth at nginx layer
    httpAdminRoot: '/',
    httpNodeRoot: '/',
    httpStatic: '/data/static/',
    ui: { path: 'ui' },
    functionGlobalContext: {},
    exportGlobalContextKeys: false,
    logging: {
        console: {
            level: "info",
            metrics: false,
            audit: false
        }
    },
    editorTheme: {
        projects: {
            enabled: {{ nodered_enable_projects | default('true') }}
        }
    }
};
