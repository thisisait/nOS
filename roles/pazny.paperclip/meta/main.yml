---
galaxy_info:
  role_name: paperclip
  namespace: pazny
  author: Pázny
  description: Paperclip multi-agent orchestration in Docker compose override (devBoxNOS devops stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — Paperclip bezi jako soucast devops stacku.
# Vyzaduje bezici postgres (pazny.postgresql role, vytvorena Unit 4)
# a openclaw host-side daemon (pazny.openclaw role, vytvorena Unit 13).
dependencies: []

collections: []
