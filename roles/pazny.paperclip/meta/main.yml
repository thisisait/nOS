---
galaxy_info:
  role_name: paperclip
  namespace: pazny
  author: Pázny
  description: Paperclip multi-agent orchestration in Docker compose override (nOS devops stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Paperclip runs as part of the devops stack.
# Requires a running postgres (pazny.postgresql role, created in Unit 4)
# and the openclaw host-side daemon (pazny.openclaw role, created in Unit 13).
dependencies: []

collections: []
