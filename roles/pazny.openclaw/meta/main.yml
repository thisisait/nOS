---
galaxy_info:
  role_name: openclaw
  namespace: pazny
  author: Pázny
  description: OpenClaw (Inspektor Klepitko) — self-hosted AI agent with Ollama backend (nOS)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Homebrew, Node.js (NVM) and nginx are managed
# outside this role in main.yml. Source file/openclaw/ and files/project-openclaw/
# stay in the playbook repo.
dependencies: []

collections:
  - community.general  # community.general.homebrew
