---
galaxy_info:
  role_name: erpnext
  namespace: pazny
  author: Pázny
  description: ERPNext (Frappe framework) in Docker compose override (nOS b2b stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — ERPNext consumes MariaDB + Redis from the infra stack.
# Orchestration (compose up sequencing, post-start recovery) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
