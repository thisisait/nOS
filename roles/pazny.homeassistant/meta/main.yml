---
galaxy_info:
  role_name: homeassistant
  namespace: pazny
  author: Pázny
  description: Home Assistant home automation in Docker compose override (devBoxNOS iiab stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — HA je samostatna Docker sluzba.
# Orchestrace (compose up + post-start onboarding) je v tasks/stacks/stack-up.yml.
dependencies: []

collections: []
