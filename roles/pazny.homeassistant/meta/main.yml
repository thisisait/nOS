---
galaxy_info:
  role_name: homeassistant
  namespace: pazny
  author: Pázny
  description: Home Assistant home automation in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — HA is a standalone Docker service.
# Orchestration (compose up + post-start onboarding) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
