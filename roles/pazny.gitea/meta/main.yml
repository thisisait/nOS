---
galaxy_info:
  role_name: gitea
  namespace: pazny
  author: Pázny
  description: Gitea in Docker compose override (nOS devops stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Gitea runs as part of the devops stack alongside
# Woodpecker / GitLab / Paperclip. Orchestrace (compose up sequencing)
# lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
