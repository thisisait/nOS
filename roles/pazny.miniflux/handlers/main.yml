---
# pazny.miniflux — handlers
# Triggered via notify: after the override template is re-rendered.

- name: Restart miniflux
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p iiab
    restart miniflux
  failed_when: false
