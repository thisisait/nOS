---
galaxy_info:
  role_name: miniflux
  namespace: pazny
  author: Pázny
  description: Miniflux RSS reader in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Miniflux consumes PostgreSQL from the infra stack.
dependencies: []

collections: []
