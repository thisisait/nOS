---
galaxy_info:
  role_name: kiwix
  namespace: pazny
  author: Pázny
  description: Kiwix offline knowledge server in Docker compose override (devBoxNOS iiab stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — Kiwix je cista Docker sluzba bez DB zavislosti.
# Orchestrace (compose up sequencing) je v tasks/stacks/stack-up.yml.
dependencies: []

collections: []
