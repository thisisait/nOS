---
galaxy_info:
  role_name: kiwix
  namespace: pazny
  author: Pázny
  description: Kiwix offline knowledge server in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Kiwix is a pure Docker service without DB dependencies.
# Orchestration (compose up sequencing) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
