---
galaxy_info:
  role_name: redis
  namespace: pazny
  author: Pázny
  description: Redis in Docker compose override (devBoxNOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — tato role je sdileny cache/broker zaklad pro
# Authentik, Infisical, n8n a dalsi. Orchestrace (compose up sequencing)
# je v tasks/stacks/core-up.yml.
dependencies: []

collections: []
