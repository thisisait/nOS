---
galaxy_info:
  role_name: redis
  namespace: pazny
  author: Pázny
  description: Redis in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — this role is the shared cache/broker base for
# Authentik, Infisical, n8n and others. Orchestration (compose up sequencing)
# lives in tasks/stacks/core-up.yml.
dependencies: []

collections: []
