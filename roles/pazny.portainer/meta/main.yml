---
galaxy_info:
  role_name: portainer
  namespace: pazny
  author: Pázny
  description: Portainer CE in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Portainer runs as part of the infra stack.
# Docker-socket-proxy stays in the base infra compose (sdileny s Traefikem).
dependencies: []

collections: []
