---
# ==============================================================================
# pazny.freescout — FreeScout helpdesk in Docker (b2b compose stack)
#
# Credentials (freescout_db_password, freescout_admin_password) staysji v
# default.credentials.yml — centralized for prefix rotation.
# ==============================================================================

freescout_version: "php8.3-1.17.159"         # tiredofit img build; bundles FreeScout 1.8.219 (>=1.8.212; fixes REM-069/070/071); aligned with default.config.yml (source of truth — bump there too)
freescout_domain: "helpdesk{{ _host_alias_seg | default('') }}.{{ tenant_domain | default('dev.local') }}"
freescout_port: 8090
freescout_data_dir: "{{ ansible_facts['env']['HOME'] }}/freescout/data"
freescout_db_name: "freescout"
freescout_db_user: "freescout"
freescout_admin_email: "{{ default_admin_email }}"
freescout_timezone: "Europe/Prague"
freescout_mem_limit: "{{ docker_mem_limit_light | default('512m') }}"
freescout_cpus: "{{ docker_cpus_light | default('0.5') }}"
