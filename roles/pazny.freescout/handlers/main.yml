---
# pazny.freescout — handlers
# Triggered via notify: after the override template is re-rendered.

- name: Restart freescout
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p b2b
    restart freescout
  failed_when: false
