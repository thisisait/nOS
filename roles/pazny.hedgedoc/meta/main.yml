---
galaxy_info:
  role_name: hedgedoc
  namespace: pazny
  author: Pázny
  description: HedgeDoc real-time collaborative markdown editor in Docker compose override (nOS b2b stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — HedgeDoc consumes PostgreSQL from the infra stack.
dependencies: []

collections: []
