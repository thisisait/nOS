---
galaxy_info:
  role_name: iiab_terminal
  namespace: pazny
  author: Pázny
  description: IIAB Terminal — SSH TUI hub (Python Textual) pro nOS
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Python/pyenv and SSH are managed outside this role.
# Handler "Restart ssh" stays play-level in main.yml (shared); the role
# it just notifies — Ansible looks up handlers globally.
# Tasks use only ansible.builtin, no extra collections.
dependencies: []
