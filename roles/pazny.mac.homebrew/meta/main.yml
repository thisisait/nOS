---
# Track H Phase 2 (2026-05-01): added meta/main.yml so the role advertises
# its ansible-core floor + macOS-only platform like every other pazny.*
# role. The role wraps Homebrew bootstrap + tap registration + package
# install loop on Apple Silicon.

dependencies: []

galaxy_info:
  role_name: homebrew
  namespace: pazny
  author: Pázny
  description: Homebrew bootstrap + tap + package management for Apple Silicon (nOS host roles).
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - macos
    - homebrew
    - package
