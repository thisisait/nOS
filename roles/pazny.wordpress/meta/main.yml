---
galaxy_info:
  role_name: wordpress
  namespace: pazny
  author: Pázny
  description: WordPress in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — the role belongs to the iiab stack, which is
# orchestrovan v tasks/stacks/stack-up.yml. Role zavisi na pazny.mariadb
# but orchestration is handled at stack-up.yml level (infra first), not
# role meta dependencies (to prevent double include during an iiab run).
dependencies: []

collections: []
