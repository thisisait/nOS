---
# pazny.tempo — handlers
# Role-local "Restart tempo" — may be overridden by a play-level handler in main.yml.

- name: Restart tempo
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p observability
    restart tempo
  failed_when: false
