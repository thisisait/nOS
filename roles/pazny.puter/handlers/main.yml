---
# pazny.puter — handlers

- name: Restart puter
  ansible.builtin.shell: >
    {{ docker_bin }} compose -f "{{ stacks_dir }}/iiab/docker-compose.yml" -p iiab
    restart puter
  failed_when: false
