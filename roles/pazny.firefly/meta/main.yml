---
galaxy_info:
  role_name: firefly
  namespace: pazny
  author: Pázny
  description: Firefly III personal finance manager in Docker compose override (nOS b2b stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Firefly consumes MariaDB + Redis from the infra stack
# and the Authentik proxy outpost for SSO (remote_user_guard).
dependencies: []

collections: []
