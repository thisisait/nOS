---
galaxy_info:
  role_name: jellyfin
  namespace: pazny
  author: Pázny
  description: Jellyfin media server in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Jellyfin is a standalone Docker service.
# Orchestration (compose up + post-start wizard) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
