---
galaxy_info:
  role_name: ears
  namespace: pazny
  author: Pázny
  description: "Ears — the estate speech organ. Hears a turn, has the caddy answer it, reads the answer back (parakeet-mlx + say)."
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies. Python and ffmpeg come from outside the role
# (tasks/python.yml, homebrew_installed_packages); the agent definitions and
# the DataTables are seeded by pazny.keap.
dependencies: []
