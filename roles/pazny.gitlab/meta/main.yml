---
galaxy_info:
  role_name: gitlab
  namespace: pazny
  author: Pázny
  description: GitLab CE in Docker compose override (nOS devops stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — GitLab runs as part of the devops stack and has its own
# Omnibus bundle (PostgreSQL, Redis, Sidekiq). It therefore does not need our postgres
# roli. Orchestrace lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
