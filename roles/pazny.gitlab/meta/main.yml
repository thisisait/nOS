---
galaxy_info:
  role_name: gitlab
  namespace: pazny
  author: Pázny
  description: GitLab CE in Docker compose override (devBoxNOS devops stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — GitLab bezi jako soucast devops stacku, ma vlastni
# Omnibus bundle (PostgreSQL, Redis, Sidekiq). Nepotrebuje tedy nase postgres
# roli. Orchestrace je v tasks/stacks/stack-up.yml.
dependencies: []

collections: []
