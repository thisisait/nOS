---
galaxy_info:
  role_name: calibre_web
  namespace: pazny
  author: Pázny
  description: Calibre-Web ebook server in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Calibre-Web is a standalone Docker service.
# Orchestration (compose up + post-start password reset) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
