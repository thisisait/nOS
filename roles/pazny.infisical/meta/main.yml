---
galaxy_info:
  role_name: infisical
  namespace: pazny
  author: Pázny
  description: Infisical CE secrets vault in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Infisical depends on Postgres + Redis (infra
# stack) which currently stay in base templates/stacks/infra/
# docker-compose.yml.j2 (Wave 2.2 Unit 4). Orchestrace sekvencovani je
# v tasks/stacks/core-up.yml.
dependencies: []

collections: []
