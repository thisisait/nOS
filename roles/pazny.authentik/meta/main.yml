---
galaxy_info:
  role_name: authentik
  namespace: pazny
  author: Pázny
  description: Authentik SSO (server + worker) + blueprints in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Authentik depends on Postgres + Redis (infra
# stack) which currently stay in base templates/stacks/infra/
# docker-compose.yml.j2 (Wave 2.2 Unit 4). Sequencing orchestration lives
# in tasks/stacks/core-up.yml.
dependencies: []

collections: []
