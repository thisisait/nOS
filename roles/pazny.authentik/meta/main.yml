---
galaxy_info:
  role_name: authentik
  namespace: pazny
  author: Pázny
  description: Authentik SSO (server + worker) + blueprints in Docker compose override (devBoxNOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — Authentik zavisi na Postgres + Redis (infra
# stack), ktere zatim zustavaji v base templates/stacks/infra/
# docker-compose.yml.j2 (Wave 2.2 Unit 4). Orchestrace sekvencovani je
# v tasks/stacks/core-up.yml.
dependencies: []

collections: []
