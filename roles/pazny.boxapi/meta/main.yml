---
galaxy_info:
  role_name: boxapi
  namespace: pazny
  author: Pázny
  description: Box Management API (FastAPI + uvicorn via launchd) pro nOS
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Python3 and Homebrew are installed outside the role.
# Source files/boxapi/ and templates/boxapi-launchd.plist.j2 stay
# v playbook repo.
# Tasks use only ansible.builtin, no extra collections.
dependencies: []
