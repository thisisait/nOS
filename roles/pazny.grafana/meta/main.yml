---
galaxy_info:
  role_name: grafana
  namespace: pazny
  author: Pázny
  description: Grafana in Docker compose override (nOS observability stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Grafana is started as part of the observability
# stack alongside Prometheus / Loki / Tempo, which stay in base
# templates/stacks/observability/docker-compose.yml.j2. After extraction
# into separate roles (Wave 2.2+) we add meta dependencies.
dependencies: []

collections: []
