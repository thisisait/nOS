---
galaxy_info:
  role_name: linux_systemd_user
  author: pazny
  description: "systemd --user service manager (Linux sibling of launchctl --user)."
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags:
    - systemd
    - linux
    - service
    - daemon

dependencies: []
