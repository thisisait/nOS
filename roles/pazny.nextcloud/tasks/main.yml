---
# ==============================================================================
# pazny.nextcloud/tasks/main.yml
#
# Called from tasks/stacks/stack-up.yml BEFORE `docker compose -p iiab up`:
# creates the data dir, nginx vhost, renders the compose override fragment do
# {{ stacks_dir }}/iiab/overrides/nextcloud.yml, which stack-up.yml then
# collects via `find` and passes as an additional -f flag.
#
# Post-start DB password sync + admin reconverge + trusted_domains je
# v tasks/post.yml — called from stack-up.yml az PO compose up.
# ==============================================================================

- name: "[pazny.nextcloud] Ensure directories exist"
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0750'
  loop:
    - "{{ nextcloud_dir }}"

# The data dir is mounted at /data and written by www-data (33) directly — the
# entrypoint takes over /var/www/html, never /data. Operator-owned 0750 made
# the installer loop "Cannot create or write into the data directory /data"
# on Linux (cloud lane, 2026-09-26). macOS: file sharing maps ownership.
- name: "[pazny.nextcloud] Ensure the data directory exists, owned by www-data"
  ansible.builtin.file:
    path: "{{ nextcloud_data_dir }}"
    state: directory
    mode: '0750'
    owner: "{{ omit if ansible_os_family == 'Darwin' else nextcloud_www_uid | default(33) }}"
    group: "{{ omit if ansible_os_family == 'Darwin' else nextcloud_www_uid | default(33) }}"
  become: "{{ ansible_os_family != 'Darwin' }}"

# The overlay below renders into config/ BEFORE the first compose up — on a
# fresh machine nothing has created it yet. Measured 2026-09-26 on the cloud
# lane, twice: first "Destination directory …/nextcloud/config does not
# exist"; then, once created by us, the installer failed "Cannot write into
# config directory" — the entrypoint does NOT take over a directory it finds,
# and it runs as www-data (uid/gid 33 in the Debian image). Linux: owned by 33.
# macOS: Docker Desktop's file sharing maps ownership, so no chown (and no
# become) there. No `mode`: Nextcloud manages what it writes inside.
- name: "[pazny.nextcloud] Ensure the config directory exists, writable by www-data"
  ansible.builtin.file:
    path: "{{ nextcloud_dir }}/config"
    state: directory
    owner: "{{ omit if ansible_os_family == 'Darwin' else nextcloud_www_uid | default(33) }}"
    group: "{{ omit if ansible_os_family == 'Darwin' else nextcloud_www_uid | default(33) }}"
  become: "{{ ansible_os_family != 'Darwin' }}"

- name: "[pazny.nextcloud] Render database-TLS config overlay"
  # Rendered BEFORE compose up, into the bind-mounted config dir, so the value
  # is present the first time the container reads it. Nextcloud merges every
  # *.config.php over config.php (lib/private/Config.php:194), which is what
  # lets this be authoritative without occ — and without a working database.
  ansible.builtin.template:
    src: nos-db-tls.config.php.j2
    dest: "{{ nextcloud_dir }}/config/nos-db-tls.config.php"
    mode: '0644'

- name: "[pazny.nextcloud] Render compose override fragment"
  ansible.builtin.template:
    src: compose.yml.j2
    dest: "{{ stacks_dir }}/iiab/overrides/nextcloud.yml"
    mode: '0600'
  notify: Restart nextcloud

# Linux routes via Traefik; host-nginx per-service vhosts are macOS-only.
- name: "[pazny.nextcloud] Deploy nginx vhost"
  ansible.builtin.template:
    src: "{{ playbook_dir }}/templates/nginx/sites-available/nextcloud.conf"
    dest: "{{ homebrew_prefix }}/etc/nginx/sites-available/nextcloud.conf"
    mode: '0644'
  notify: Restart nginx
  when:
    - install_nginx | default(true)
    - ansible_os_family == 'Darwin'

- name: "[pazny.nextcloud] Enable nginx vhost (symlink)"
  ansible.builtin.file:
    src: "{{ homebrew_prefix }}/etc/nginx/sites-available/nextcloud.conf"
    dest: "{{ homebrew_prefix }}/etc/nginx/sites-enabled/nextcloud.conf"
    state: link
    force: true
  notify: Restart nginx
  when:
    - install_nginx | default(true)
    - ansible_os_family == 'Darwin'

- name: "[pazny.nextcloud] Print installation info"
  ansible.builtin.debug:
    msg: |
      ══════════════════════════════════════════════════════
       Nextcloud (Docker) ready
      ══════════════════════════════════════════════════════
       URL:       https://{{ nextcloud_domain }}
       Container: nextcloud → 127.0.0.1:{{ nextcloud_port | default(8085) }}
       Data:      {{ nextcloud_data_dir }}
       DB:        {{ nextcloud_db_name }} @ mariadb (iiab_net)

       Add to /etc/hosts:   127.0.0.1 {{ nextcloud_domain }}
      ══════════════════════════════════════════════════════
