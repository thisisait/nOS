---
galaxy_info:
  role_name: bluesky_pds
  namespace: pazny
  author: Pázny
  description: Bluesky PDS (AT Protocol) in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — PDS runs as part of the infra stack. Identity
# bridge to Authentik stays in tasks/stacks/bluesky_pds_bridge.yml
# (cross-service, not role-owned).
dependencies: []

collections: []
