---
galaxy_info:
  role_name: influxdb
  namespace: pazny
  author: Pázny
  description: InfluxDB 2.x time-series database in Docker compose override (nOS observability stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — InfluxDB is started as part of the observability
# stack alongside Grafana / Prometheus / Loki / Tempo. Orchestration (compose up
# sequencing) lives in tasks/stacks/core-up.yml.
dependencies: []

collections: []
