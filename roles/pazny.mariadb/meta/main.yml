---
galaxy_info:
  role_name: mariadb
  namespace: pazny
  author: Pázny
  description: MariaDB in Docker compose override (nOS infra stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — this role is the shared data base for
# wordpress/nextcloud/erpnext/freescout/freepbx. Orchestrace (compose
# up sequencing, post-start sequence) lives in tasks/stacks/core-up.yml.
dependencies: []

collections:
  - community.mysql  # mysql_info, mysql_db, mysql_user v post.yml
