---
galaxy_info:
  role_name: prometheus
  namespace: pazny
  author: Pázny
  description: Prometheus in Docker compose override (nOS observability stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Prometheus is started as part of the observability
# stack alongside Grafana / Loki / Tempo. Orchestration (compose up sequencing,
# post-start) lives in tasks/stacks/core-up.yml.
dependencies: []

collections: []
