---
galaxy_info:
  role_name: prometheus
  namespace: pazny
  author: Pázny
  description: Prometheus in Docker compose override (devBoxNOS observability stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Zadne role dependencies — Prometheus se spousti jako soucast observability
# stacku vedle Grafana / Loki / Tempo. Orchestrace (compose up sequencing,
# post-start) je v tasks/stacks/core-up.yml.
dependencies: []

collections: []
