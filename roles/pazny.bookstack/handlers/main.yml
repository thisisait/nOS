---
# pazny.bookstack — handlers
# Triggered by notify: after the override template is re-rendered.

- name: Restart bookstack
  ansible.builtin.shell: >
    {{ docker_bin }} compose -f "{{ stacks_dir }}/b2b/docker-compose.yml" -p b2b
    restart bookstack
  failed_when: false
