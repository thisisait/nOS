---
# pazny.outline — handlers
# Volano z notify: po re-renderu override sablony.

- name: Restart outline
  ansible.builtin.shell: >
    {{ docker_bin }} compose -f "{{ stacks_dir }}/b2b/docker-compose.yml" -p b2b
    restart outline
  failed_when: false
