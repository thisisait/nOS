---
galaxy_info:
  role_name: outline
  namespace: pazny
  author: Pázny
  description: Outline team wiki in Docker compose override (nOS b2b stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — Outline consumes PostgreSQL + Redis from the infra stack.
dependencies: []

collections: []
