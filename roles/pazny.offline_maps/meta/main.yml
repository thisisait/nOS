---
galaxy_info:
  role_name: offline_maps
  namespace: pazny
  author: Pázny
  description: Tileserver-gl offline maps in Docker compose override (nOS iiab stack)
  license: MIT
  min_ansible_version: "2.20"
  platforms:
    - name: MacOSX
      versions:
        - all

# No role dependencies — the tileserver is a standalone Docker service.
# Orchestration (compose up) lives in tasks/stacks/stack-up.yml.
dependencies: []

collections: []
