# ===========================================================================
# report-chain-synthetic — answer key for the `report-prepare` task family.
#
# GENERATED by tools/report-chain-synth-gen.py from SCHEMA ONLY:
#   state/keap-tables/*.table.yml, the cortex relation-type ontology, and
#   the frozen opcode registry in cortex-opcodes.ts. No client data, real
#   or historical, was read to produce it; every name below is synthetic.
#   Do not hand-edit — regenerate.
#
# cases: id -> the NL-ish request and the MINIMAL correct chain.
#   tables    — the set report-chain-planning-bench's code oracle diffs
#               against what a model actually queried (superset with a
#               justification = pass, missing = fail).
#   relations — the `rel:` edge type the bundle hangs on.
#   chain     — ordered opcode+operand steps; opcodes are the frozen set.
# ===========================================================================

count: 30
cases:
  invoice-verify-queue-001:
    report_type: invoice-verify-queue
    request: "what is still unverified on Firma Vzor s.r.o.'s books before we report"
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-review, party, pending-invoice-verify]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: pending-invoice-verify }
      - { op: get, operand: invoice-review }
      - { op: filter, operand: invoice.verified }
      - { op: review, operand: invoice-review.slug }
  ongoing-books-002:
    report_type: ongoing-books
    request: "prepare the ongoing report for client Firma Vzor s.r.o."
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [account, journal-entry, party, posting]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: account }
      - { op: filter, operand: account.party }
      - { op: get, operand: posting }
      - { op: filter, operand: posting.account }
      - { op: get, operand: journal-entry }
      - { op: rank, operand: journal-entry.date }
      - { op: map, operand: posting.amount }
  party-registry-003:
    report_type: party-registry
    request: "check the registry and contact details we hold for Firma Vzor s.r.o."
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [party, party-address, party-contact, party-registry-status, party-tax-identity]
    relations: [defines]
    chain:
      - { op: resolve, operand: party.slug }
      - { op: get, operand: party-registry-status }
      - { op: filter, operand: party-registry-status.party }
      - { op: get, operand: party-tax-identity }
      - { op: get, operand: party-address }
      - { op: get, operand: party-contact }
      - { op: classify, operand: party-registry-status.vat_reliability }
  print-throughput-004:
    report_type: print-throughput
    request: "prepare the production report for Firma Vzor s.r.o.'s outstanding print orders"
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [party, print-job, print-job-step, print-material, print-order]
    relations: [prerequisite-for]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: print-order }
      - { op: filter, operand: print-order.customer }
      - { op: get, operand: print-job }
      - { op: filter, operand: print-job.order }
      - { op: get, operand: print-job-step }
      - { op: get, operand: print-material }
      - { op: rank, operand: print-job-step.planned_start }
  project-delivery-005:
    report_type: project-delivery
    request: "prepare the delivery report on the Firma Vzor s.r.o. engagement"
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [kolben-engineer, kolben-project, kolben-ticket, kolben-time-entry, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: kolben-project }
      - { op: filter, operand: kolben-project.client }
      - { op: get, operand: kolben-ticket }
      - { op: filter, operand: kolben-ticket.project }
      - { op: get, operand: kolben-time-entry }
      - { op: get, operand: kolben-engineer }
      - { op: map, operand: kolben-time-entry.hours }
      - { op: rank, operand: kolben-ticket.priority }
  receivables-006:
    report_type: receivables
    request: "what does Firma Vzor s.r.o. still owe us — pull the receivables report"
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.buyer }
      - { op: get, operand: invoice-line }
      - { op: filter, operand: invoice-line.invoice }
      - { op: rank, operand: invoice.due_date }
      - { op: map, operand: invoice.payable_amount }
  vat-summary-007:
    report_type: vat-summary
    request: "prepare the quarterly VAT summary for Firma Vzor s.r.o."
    client: "Firma Vzor s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [derived-from]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: invoice-line }
      - { op: classify, operand: invoice.vat_regime }
      - { op: map, operand: invoice.vat_breakdown }
  invoice-verify-queue-008:
    report_type: invoice-verify-queue
    request: "what is still unverified on Vzorová Dílna a.s.'s books before we report"
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [invoice, invoice-review, party, pending-invoice-verify]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: pending-invoice-verify }
      - { op: get, operand: invoice-review }
      - { op: filter, operand: invoice.verified }
      - { op: review, operand: invoice-review.slug }
  ongoing-books-009:
    report_type: ongoing-books
    request: "prepare the ongoing report for client Vzorová Dílna a.s."
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [account, journal-entry, party, posting]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: account }
      - { op: filter, operand: account.party }
      - { op: get, operand: posting }
      - { op: filter, operand: posting.account }
      - { op: get, operand: journal-entry }
      - { op: rank, operand: journal-entry.date }
      - { op: map, operand: posting.amount }
  party-registry-010:
    report_type: party-registry
    request: "check the registry and contact details we hold for Vzorová Dílna a.s."
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [party, party-address, party-contact, party-registry-status, party-tax-identity]
    relations: [defines]
    chain:
      - { op: resolve, operand: party.slug }
      - { op: get, operand: party-registry-status }
      - { op: filter, operand: party-registry-status.party }
      - { op: get, operand: party-tax-identity }
      - { op: get, operand: party-address }
      - { op: get, operand: party-contact }
      - { op: classify, operand: party-registry-status.vat_reliability }
  print-throughput-011:
    report_type: print-throughput
    request: "prepare the production report for Vzorová Dílna a.s.'s outstanding print orders"
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [party, print-job, print-job-step, print-material, print-order]
    relations: [prerequisite-for]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: print-order }
      - { op: filter, operand: print-order.customer }
      - { op: get, operand: print-job }
      - { op: filter, operand: print-job.order }
      - { op: get, operand: print-job-step }
      - { op: get, operand: print-material }
      - { op: rank, operand: print-job-step.planned_start }
  project-delivery-012:
    report_type: project-delivery
    request: "prepare the delivery report on the Vzorová Dílna a.s. engagement"
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [kolben-engineer, kolben-project, kolben-ticket, kolben-time-entry, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: kolben-project }
      - { op: filter, operand: kolben-project.client }
      - { op: get, operand: kolben-ticket }
      - { op: filter, operand: kolben-ticket.project }
      - { op: get, operand: kolben-time-entry }
      - { op: get, operand: kolben-engineer }
      - { op: map, operand: kolben-time-entry.hours }
      - { op: rank, operand: kolben-ticket.priority }
  receivables-013:
    report_type: receivables
    request: "what does Vzorová Dílna a.s. still owe us — pull the receivables report"
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.buyer }
      - { op: get, operand: invoice-line }
      - { op: filter, operand: invoice-line.invoice }
      - { op: rank, operand: invoice.due_date }
      - { op: map, operand: invoice.payable_amount }
  vat-summary-014:
    report_type: vat-summary
    request: "prepare the quarterly VAT summary for Vzorová Dílna a.s."
    client: "Vzorová Dílna a.s."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [derived-from]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: invoice-line }
      - { op: classify, operand: invoice.vat_regime }
      - { op: map, operand: invoice.vat_breakdown }
  invoice-verify-queue-015:
    report_type: invoice-verify-queue
    request: "what is still unverified on Ukázka Trading s.r.o.'s books before we report"
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-review, party, pending-invoice-verify]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: pending-invoice-verify }
      - { op: get, operand: invoice-review }
      - { op: filter, operand: invoice.verified }
      - { op: review, operand: invoice-review.slug }
  ongoing-books-016:
    report_type: ongoing-books
    request: "prepare the ongoing report for client Ukázka Trading s.r.o."
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [account, journal-entry, party, posting]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: account }
      - { op: filter, operand: account.party }
      - { op: get, operand: posting }
      - { op: filter, operand: posting.account }
      - { op: get, operand: journal-entry }
      - { op: rank, operand: journal-entry.date }
      - { op: map, operand: posting.amount }
  party-registry-017:
    report_type: party-registry
    request: "check the registry and contact details we hold for Ukázka Trading s.r.o."
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [party, party-address, party-contact, party-registry-status, party-tax-identity]
    relations: [defines]
    chain:
      - { op: resolve, operand: party.slug }
      - { op: get, operand: party-registry-status }
      - { op: filter, operand: party-registry-status.party }
      - { op: get, operand: party-tax-identity }
      - { op: get, operand: party-address }
      - { op: get, operand: party-contact }
      - { op: classify, operand: party-registry-status.vat_reliability }
  print-throughput-018:
    report_type: print-throughput
    request: "prepare the production report for Ukázka Trading s.r.o.'s outstanding print orders"
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [party, print-job, print-job-step, print-material, print-order]
    relations: [prerequisite-for]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: print-order }
      - { op: filter, operand: print-order.customer }
      - { op: get, operand: print-job }
      - { op: filter, operand: print-job.order }
      - { op: get, operand: print-job-step }
      - { op: get, operand: print-material }
      - { op: rank, operand: print-job-step.planned_start }
  project-delivery-019:
    report_type: project-delivery
    request: "prepare the delivery report on the Ukázka Trading s.r.o. engagement"
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [kolben-engineer, kolben-project, kolben-ticket, kolben-time-entry, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: kolben-project }
      - { op: filter, operand: kolben-project.client }
      - { op: get, operand: kolben-ticket }
      - { op: filter, operand: kolben-ticket.project }
      - { op: get, operand: kolben-time-entry }
      - { op: get, operand: kolben-engineer }
      - { op: map, operand: kolben-time-entry.hours }
      - { op: rank, operand: kolben-ticket.priority }
  receivables-020:
    report_type: receivables
    request: "what does Ukázka Trading s.r.o. still owe us — pull the receivables report"
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.buyer }
      - { op: get, operand: invoice-line }
      - { op: filter, operand: invoice-line.invoice }
      - { op: rank, operand: invoice.due_date }
      - { op: map, operand: invoice.payable_amount }
  vat-summary-021:
    report_type: vat-summary
    request: "prepare the quarterly VAT summary for Ukázka Trading s.r.o."
    client: "Ukázka Trading s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [derived-from]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: invoice-line }
      - { op: classify, operand: invoice.vat_regime }
      - { op: map, operand: invoice.vat_breakdown }
  invoice-verify-queue-022:
    report_type: invoice-verify-queue
    request: "what is still unverified on Model Podnik s.r.o.'s books before we report"
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-review, party, pending-invoice-verify]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: pending-invoice-verify }
      - { op: get, operand: invoice-review }
      - { op: filter, operand: invoice.verified }
      - { op: review, operand: invoice-review.slug }
  ongoing-books-023:
    report_type: ongoing-books
    request: "prepare the ongoing report for client Model Podnik s.r.o."
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [account, journal-entry, party, posting]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: account }
      - { op: filter, operand: account.party }
      - { op: get, operand: posting }
      - { op: filter, operand: posting.account }
      - { op: get, operand: journal-entry }
      - { op: rank, operand: journal-entry.date }
      - { op: map, operand: posting.amount }
  party-registry-024:
    report_type: party-registry
    request: "check the registry and contact details we hold for Model Podnik s.r.o."
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [party, party-address, party-contact, party-registry-status, party-tax-identity]
    relations: [defines]
    chain:
      - { op: resolve, operand: party.slug }
      - { op: get, operand: party-registry-status }
      - { op: filter, operand: party-registry-status.party }
      - { op: get, operand: party-tax-identity }
      - { op: get, operand: party-address }
      - { op: get, operand: party-contact }
      - { op: classify, operand: party-registry-status.vat_reliability }
  print-throughput-025:
    report_type: print-throughput
    request: "prepare the production report for Model Podnik s.r.o.'s outstanding print orders"
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [party, print-job, print-job-step, print-material, print-order]
    relations: [prerequisite-for]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: print-order }
      - { op: filter, operand: print-order.customer }
      - { op: get, operand: print-job }
      - { op: filter, operand: print-job.order }
      - { op: get, operand: print-job-step }
      - { op: get, operand: print-material }
      - { op: rank, operand: print-job-step.planned_start }
  project-delivery-026:
    report_type: project-delivery
    request: "prepare the delivery report on the Model Podnik s.r.o. engagement"
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [kolben-engineer, kolben-project, kolben-ticket, kolben-time-entry, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: kolben-project }
      - { op: filter, operand: kolben-project.client }
      - { op: get, operand: kolben-ticket }
      - { op: filter, operand: kolben-ticket.project }
      - { op: get, operand: kolben-time-entry }
      - { op: get, operand: kolben-engineer }
      - { op: map, operand: kolben-time-entry.hours }
      - { op: rank, operand: kolben-ticket.priority }
  receivables-027:
    report_type: receivables
    request: "what does Model Podnik s.r.o. still owe us — pull the receivables report"
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.buyer }
      - { op: get, operand: invoice-line }
      - { op: filter, operand: invoice-line.invoice }
      - { op: rank, operand: invoice.due_date }
      - { op: map, operand: invoice.payable_amount }
  vat-summary-028:
    report_type: vat-summary
    request: "prepare the quarterly VAT summary for Model Podnik s.r.o."
    client: "Model Podnik s.r.o."   # SYNTHETIC
    tables: [invoice, invoice-line, party]
    relations: [derived-from]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: invoice-line }
      - { op: classify, operand: invoice.vat_regime }
      - { op: map, operand: invoice.vat_breakdown }
  invoice-verify-queue-029:
    report_type: invoice-verify-queue
    request: "what is still unverified on Testovací Sklad a.s.'s books before we report"
    client: "Testovací Sklad a.s."   # SYNTHETIC
    tables: [invoice, invoice-review, party, pending-invoice-verify]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: invoice }
      - { op: filter, operand: invoice.book_owner }
      - { op: get, operand: pending-invoice-verify }
      - { op: get, operand: invoice-review }
      - { op: filter, operand: invoice.verified }
      - { op: review, operand: invoice-review.slug }
  ongoing-books-030:
    report_type: ongoing-books
    request: "prepare the ongoing report for client Testovací Sklad a.s."
    client: "Testovací Sklad a.s."   # SYNTHETIC
    tables: [account, journal-entry, party, posting]
    relations: [depends-on]
    chain:
      - { op: resolve, operand: party.legal_name }
      - { op: get, operand: account }
      - { op: filter, operand: account.party }
      - { op: get, operand: posting }
      - { op: filter, operand: posting.account }
      - { op: get, operand: journal-entry }
      - { op: rank, operand: journal-entry.date }
      - { op: map, operand: posting.amount }
