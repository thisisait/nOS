# =============================================================================
# DATA STACK – BI / analytika (Metabase, Superset)
# Generováno Ansiblem – NEUPRAVUJ ručně
# Sdílené DB z infra stacku: PostgreSQL + Redis
#
# Služby extrahované do rolí (compose override fragments merged via
# tasks/stacks/stack-up.yml find+merge loop):
#   • metabase → roles/pazny.metabase/templates/compose.yml.j2
#   • superset → roles/pazny.superset/templates/compose.yml.j2
# =============================================================================

services: {}

networks:
  data_net:
    driver: bridge
  {{ stacks_shared_network }}:
    external: true
