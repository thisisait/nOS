# =============================================================================
# ENGINEERING STACK – GIS server (Docker-based tools)
# Desktop apps (KiCad, FreeCAD, Arduino) se instalují přes Homebrew Cask
# Generováno Ansiblem – NEUPRAVUJ ručně
#
# Služby extrahované do rolí (compose override fragments merged via
# tasks/stacks/stack-up.yml find+merge loop):
#   • qgis-server → roles/pazny.qgis_server/templates/compose.yml.j2
# =============================================================================

services: {}

networks:
  engineering_net:
    driver: bridge
  {{ stacks_shared_network }}:
    external: true
