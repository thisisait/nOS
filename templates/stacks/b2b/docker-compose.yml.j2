# =============================================================================
# B2B STACK – CRM/ERP, Helpdesk, Chat, Knowledge Base
# Generated by Ansible – DO NOT EDIT MANUALLY
#
# Shared DBs from the infra stack: MariaDB (shared_net:mariadb),
# PostgreSQL (shared_net:postgresql), Redis (shared_net:redis)
#
# Service blocks have been extracted into roles/pazny.*/ (Wave 2.2):
#   - erpnext   → roles/pazny.erpnext/templates/compose.yml.j2
#                 (6 services + named volume erpnext_sites)
#   - freescout → roles/pazny.freescout/templates/compose.yml.j2
#   - outline   → roles/pazny.outline/templates/compose.yml.j2
# Role fragments are merged via `docker compose -f <base> -f <override>...`
# in tasks/stacks/stack-up.yml (find + -f loop).
# =============================================================================

services: {}

{# Mattermost removed – no ARM64 FOSS image. Chat via Bluesky PDS or Nextcloud Talk. #}

networks:
  b2b_net:
    driver: bridge
  {{ stacks_shared_network }}:
    external: true
