# =============================================================================
# DEVOPS STACK – Gitea, Woodpecker CI, GitLab, Paperclip
# Generated by Ansible – DO NOT EDIT MANUALLY
#
# Wave 2.2 Unit 10: all services have been extracted into dedicated roles.
# This base file holds only networks: — actual service blocks are added by
# role-specific overrides in {{ stacks_dir }}/devops/overrides/*.yml, which
# stack-up.yml collects via a `find + -f` merge loop.
#
# Fragment map:
#   • gitea       → roles/pazny.gitea/templates/compose.yml.j2
#   • woodpecker  → roles/pazny.woodpecker/templates/compose.yml.j2 (server + agent)
#   • gitlab      → roles/pazny.gitlab/templates/compose.yml.j2
#   • paperclip   → roles/pazny.paperclip/templates/compose.yml.j2
#
# An empty services: dict would be rejected by docker compose, so we declare
# a placeholder via explicit {} — overrides then extend it with real services.
# =============================================================================

services: {}

networks:
  devops_net:
    driver: bridge
  {{ stacks_shared_network }}:
    external: true
