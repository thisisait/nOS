# =============================================================================
# OBSERVABILITY STACK – Grafana + Prometheus + Loki + Tempo
# Generated by Ansible – DO NOT EDIT MANUALLY
#
# Host-side services (Alloy, node_exporter, nginx/phpfpm exporters) stay on
# the host and are reachable from containers via host.docker.internal.
#
# All services have been extracted into separate roles:
#   - pazny.grafana    (Wave 2.1) → overrides/grafana.yml
#   - pazny.prometheus (Wave 2.2) → overrides/prometheus.yml
#   - pazny.loki       (Wave 2.2) → overrides/loki.yml
#   - pazny.tempo      (Wave 2.2) → overrides/tempo.yml
#
# core-up.yml merges fragments via `docker compose -f base.yml
# -f overrides/*.yml up`. This base file owns only the top-level
# `networks:` declaration, which role fragments reference by name.
# =============================================================================

networks:
  observability_net:
    driver: bridge
  {{ stacks_shared_network }}:
    external: true
