name: scout
version: 1
description: |
  nOS scout — drift / visibility agent. Reads recent events from
  wing.db, correlates with the committed state in `state/manifest.yml`,
  and reports what changed unexpectedly. Read-only across state +
  security; the operator decides whether drift is intentional.

  Distinct from conductor / remediator:
    - Conductor verifies platform health end-to-end (does everything
      respond? are jobs registered? is the audit trail flowing?).
    - Remediator analyzes individual security findings.
    - Scout looks at the *deltas*: tracks event types, frequency,
      actor distribution, and flags anomalies (new actor_id appearing,
      drop in conductor heartbeat, pulse job exit_code skew).

model:
  primary: claude-sonnet
  fallback: openclaw-qwen2.5-coder:32b

system_prompt_path: system.md

tools:
  - id: bash-read-only
  - id: mcp-wing
  - id: mcp-bone

multiagent:
  type: solo
  max_concurrent_threads: 1

outcomes:
  rubric_path: rubric.md
  max_iterations: 3

audit:
  capability_scopes:
    - bash.read
    - mcp.tool_use
    - wing.read
    - bone.read
    - events.write
    - audit.read
  pii_classification: none

vault:
  required_credentials:
    - scope: anthropic-api
    - scope: mcp-wing
    - scope: mcp-bone

metadata:
  ait_phase: A9.4
  ceremony_role: drift-visibility

exit_code_semantics:
  emits_sentinel: false
  exit_0: "Drift survey completed; delta report posted as events. Prose-only — exits 0 even when drift is detected (the operator decides whether drift is intentional)."
  exit_1: "Does not emit exit 1. Detected drift lives in the report body for a human decision, not in the process exit."
  exit_2: "Environment / auth / Wing error inside pulse-run-agent.sh. Fires a CRITICAL A9 notification."
