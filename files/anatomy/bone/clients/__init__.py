# Bone external-service clients. Each module wraps one Tier-1 / Tier-2
# substrate (today: Qdrant). Public class is `<Service>Client` with a
# uniform `is_configured()` predicate for endpoint guards.
