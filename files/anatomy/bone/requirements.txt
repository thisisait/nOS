fastapi>=0.115
# REM-128: pin the fastapi-transitive Starlette up to the fixed floor
# (>=1.0.1 closes the multipart/form DoS + related advisories).
starlette>=1.0.1
uvicorn[standard]>=0.32
httpx>=0.27
PyYAML>=6.0
# Track B (2026-04-26): JWT validation against Authentik JWKS for
# operational endpoints. PyJWT[crypto] pulls in `cryptography` for RS256.
# REM-110: floor >=2.13.0 closes the PyJWT cluster residuals (CVE-2026-32597
# crit-header extensions, CVE-2026-48525 unauth Base64URL-decode DoS). The
# RS/HS algorithm-confusion (CVE-2026-48526) is already code-mitigated by the
# fixed algorithms=["RS256"] allowlist in auth.py.
PyJWT[crypto]>=2.13.0
