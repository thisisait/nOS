# superset-base

Wiring layer for the Apache Superset role. **Forward-auth gate** — pivoted from
native_oidc_env (Superset configures OIDC via superset_config.py, not env).
Q2 batch B (U6). RBAC tier 2 (manager).
