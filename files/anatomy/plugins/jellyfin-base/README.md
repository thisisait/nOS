# jellyfin-base

Wiring layer for the Jellyfin role. **Forward-auth (proxy) gate** — pivoted from
original U6 native_oidc_env spec because Jellyfin OSS has no env-driven OIDC.
Q2 batch B (U6). RBAC tier 4 (guest).
