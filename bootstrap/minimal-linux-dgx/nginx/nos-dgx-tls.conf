# mkcert-issued leaf cert for <hostname>.local + every uplink IP (setup-root.sh re-issues it; CAROOT=/etc/nos/mkcert).
ssl_certificate     /etc/nginx/tls/spark.pem;
ssl_certificate_key /etc/nginx/tls/spark-key.pem;
ssl_protocols       TLSv1.2 TLSv1.3;
ssl_session_cache   shared:nosdgx:10m;
