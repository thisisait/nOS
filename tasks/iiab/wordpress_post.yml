---
# ==============================================================================
# WORDPRESS POST-START – automatická instalace (wp-cli v kontejneru)
# Běží PO docker compose up + mariadb_setup (DB musí existovat).
# ==============================================================================

- name: "[WordPress Post] Check if WordPress container is running"
  ansible.builtin.shell: >
    {{ docker_bin }} compose
    -f "{{ stacks_dir }}/iiab/docker-compose.yml"
    -p iiab ps -q wordpress 2>/dev/null
  register: _wp_container
  changed_when: false
  failed_when: false

- name: "[WordPress Post] Check if WordPress is already installed"
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p iiab
    exec -T wordpress wp core is-installed --allow-root 2>/dev/null
  register: _wp_installed
  changed_when: false
  failed_when: false
  when: _wp_container.stdout | default('') | length > 0

- name: "[WordPress Post] Install wp-cli"
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p iiab
    exec -T wordpress bash -c
    'curl -sO https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
    && chmod +x wp-cli.phar && mv wp-cli.phar /usr/local/bin/wp'
  when:
    - _wp_container.stdout | default('') | length > 0
    - _wp_installed.rc | default(1) != 0
  changed_when: true
  failed_when: false

- name: "[WordPress Post] Run WordPress auto-install"
  ansible.builtin.shell: >
    {{ docker_bin }} compose -p iiab
    exec -T wordpress wp core install --allow-root
    --url="https://{{ wordpress_domain | default('wordpress.dev.local') }}"
    --title="Dev WordPress"
    --admin_user="{{ wordpress_admin_user | default('admin') }}"
    --admin_password="{{ wordpress_admin_password | default('changeme_pw_wordpress_admin') }}"
    --admin_email="{{ wordpress_admin_email | default('admin@dev.local') }}"
    --skip-email
  when:
    - _wp_container.stdout | default('') | length > 0
    - _wp_installed.rc | default(1) != 0
  register: _wp_install
  changed_when: _wp_install.rc == 0
  failed_when: false
