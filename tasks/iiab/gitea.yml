---
# ==============================================================================
# Gitea – self-hosted Git service (Docker, devops stack)
# Data v ~/gitea (nebo SSD), konfigurace přes env vars v compose šabloně
# ==============================================================================

- name: "[Gitea] Ensure data directory exists"
  ansible.builtin.file:
    path: "{{ gitea_data_dir | default(ansible_facts['env']['HOME'] + '/gitea') }}"
    state: directory
    mode: '0755'

# Zastavit starou Homebrew Gitea (pokud běží)
- name: "[Gitea] Stop old Homebrew Gitea service"
  ansible.builtin.command: brew services stop gitea
  register: _gitea_brew_stop
  changed_when: "'Stopping' in (_gitea_brew_stop.stdout | default(''))"
  failed_when: false

- name: "[Gitea] Enable nginx vhost"
  ansible.builtin.file:
    src: "{{ homebrew_prefix }}/etc/nginx/sites-available/gitea.conf"
    dest: "{{ homebrew_prefix }}/etc/nginx/sites-enabled/gitea.conf"
    state: link
    force: true
  notify: Restart nginx
  when: install_nginx | default(true)
