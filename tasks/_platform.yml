---
# ==============================================================================
# Cross-platform variable resolution (Track C, 2026-04-26).
#
# Imported as the very first thing in main.yml's pre_tasks. Sets a small set
# of nos_* variables that abstract the macOS vs Ubuntu Linux differences
# touched by the playbook. Most roles still default to the macOS path via
# `homebrew_prefix` etc. — this layer is for the cross-platform glue
# (nginx vhost rendering, docker bin path, autostart unit dir).
#
# Variables produced (all prefixed `nos_`):
#   nos_platform              "macos" | "ubuntu" | "debian" | "redhat" | "linux"
#   nos_pkg_manager           "homebrew" | "apt" | "dnf"
#   nos_nginx_prefix          /opt/homebrew | /usr
#   nos_nginx_etc_dir         /opt/homebrew/etc/nginx | /etc/nginx
#   nos_nginx_log_dir         /opt/homebrew/var/log/nginx | /var/log/nginx
#   nos_nginx_run_user        _www | www-data
#   nos_nginx_sites_available {{ nos_nginx_etc_dir }}/sites-available
#   nos_nginx_sites_enabled   {{ nos_nginx_etc_dir }}/sites-enabled
#   nos_systemd_user_dir      ~/Library/LaunchAgents | ~/.config/systemd/user
#   nos_service_manager       "launchd" | "systemd-user" | "systemd-system"
#   nos_init                  "launchd" | "systemd" | "none"
#   nos_user_systemctl        "systemctl --user" | ~/.local/bin/nos-proc
#   nos_kernel_ipv6           false only on a Linux kernel without IPv6
#   nos_is_container          true when the kernel is someone else's (docker/lxc/…)
#   nos_is_wsl                true on Windows' WSL2 (Linux path, Microsoft kernel)
#   nos_container_user        '' (macOS) | '<uid>:<gid>' — non-root images on Linux
#   nos_docker_bin            /usr/local/bin/docker | /usr/bin/docker
#   nos_homebrew_prefix       /opt/homebrew | "" (empty on Linux)
#
# Why: 282 references to `homebrew_prefix` / `launchctl` / `Library/LaunchAgents`
# exist across the playbook. We don't rewrite all of them in Track C — this
# file is the seam future cross-platform fixes plug into. Roles that mutate
# the host (Homebrew installs, launchd loads) keep their Darwin gates and
# get Linux siblings (pazny.linux.*).
# ==============================================================================

- name: "[platform] Detect distribution flavour"
  ansible.builtin.set_fact:
    nos_platform: >-
      {{ 'macos' if ansible_os_family == 'Darwin' else
         'ubuntu' if ansible_distribution == 'Ubuntu' else
         'debian' if ansible_os_family == 'Debian' else
         'redhat' if ansible_os_family == 'RedHat' else
         'linux' }}

- name: "[platform] Set macOS variables"
  ansible.builtin.set_fact:
    nos_pkg_manager: "homebrew"
    nos_homebrew_prefix: "{{ homebrew_prefix | default('/opt/homebrew') }}"
    nos_nginx_prefix: "{{ homebrew_prefix | default('/opt/homebrew') }}"
    nos_nginx_etc_dir: "{{ homebrew_prefix | default('/opt/homebrew') }}/etc/nginx"
    nos_nginx_log_dir: "{{ homebrew_prefix | default('/opt/homebrew') }}/var/log/nginx"
    nos_nginx_run_user: "_www"
    nos_systemd_user_dir: "{{ ansible_facts['env']['HOME'] }}/Library/LaunchAgents"
    nos_service_manager: "launchd"
    nos_docker_bin: "{{ docker_bin | default('/usr/local/bin/docker') }}"
  when: nos_platform == 'macos'

- name: "[platform] Set Debian/Ubuntu variables"
  ansible.builtin.set_fact:
    nos_pkg_manager: "apt"
    nos_homebrew_prefix: ""
    nos_nginx_prefix: "/usr"
    nos_nginx_etc_dir: "/etc/nginx"
    nos_nginx_log_dir: "/var/log/nginx"
    nos_nginx_run_user: "www-data"
    nos_systemd_user_dir: "{{ ansible_facts['env']['HOME'] }}/.config/systemd/user"
    # We default to user-mode systemd so the playbook can run unprivileged
    # outside of `become: true` blocks. Hardening + docker install need root
    # via explicit `become: true` in their own roles.
    nos_service_manager: "systemd-user"
    nos_docker_bin: "/usr/bin/docker"
  when: nos_platform in ['ubuntu', 'debian']

- name: "[platform] Set RedHat/Fedora variables"
  ansible.builtin.set_fact:
    nos_pkg_manager: "dnf"
    nos_homebrew_prefix: ""
    nos_nginx_prefix: "/usr"
    nos_nginx_etc_dir: "/etc/nginx"
    nos_nginx_log_dir: "/var/log/nginx"
    nos_nginx_run_user: "nginx"
    nos_systemd_user_dir: "{{ ansible_facts['env']['HOME'] }}/.config/systemd/user"
    nos_service_manager: "systemd-user"
    nos_docker_bin: "/usr/bin/docker"
  when: nos_platform == 'redhat'

# A Linux host whose PID 1 is not systemd — a container, a cloud agent sandbox
# (Claude Code on the web runs `process_api` as PID 1), WSL1. The unit CONTRACT
# does not change: roles still render the same ~/.config/systemd/user/*.service
# files through pazny.linux.systemd_user. What changes is who reads them —
# `nos-proc` (files/anatomy/scripts/nos-proc.py), a repo-owned supervisor that
# honours ExecStart / Environment / EnvironmentFile / WorkingDirectory /
# Restart. Timers are NOT scheduled there and it says so on every start.
# docs/cloud-e2e.md §No init system.
- name: "[platform] Detect the init system (systemd vs none)"
  ansible.builtin.set_fact:
    nos_init: >-
      {{ 'launchd' if (nos_service_manager | default('')) == 'launchd'
         else ('systemd' if (ansible_facts['service_mgr'] | default('systemd')) == 'systemd'
               else 'none') }}

- name: "[platform] The user-scope service control command"
  ansible.builtin.set_fact:
    nos_user_systemctl: >-
      {{ (ansible_facts['env']['HOME'] ~ '/.local/bin/nos-proc')
         if nos_init == 'none' else 'systemctl --user' }}

# A kernel built without IPv6 (measured: the Claude cloud sandbox has no
# /proc/sys/net/ipv6) makes every container that binds `[::]` die at start —
# docker-socket-proxy's HAProxy crash-looped, and the STRICT health-wait held
# the whole infra stack on it. Linux-only probe; macOS keeps the default true.
- name: "[platform] Probe kernel IPv6 support (Linux)"
  ansible.builtin.stat:
    path: /proc/sys/net/ipv6
  register: _nos_ipv6_stat
  when: ansible_os_family != 'Darwin'

- name: "[platform] Record kernel IPv6 support"
  ansible.builtin.set_fact:
    nos_kernel_ipv6: "{{ (_nos_ipv6_stat.stat.exists | default(true)) if ansible_os_family != 'Darwin' else true }}"

# What KIND of host this is, so host-level work that only means something on a
# real machine gates itself instead of asking the operator for a toggle.
#   nos_is_container  the kernel belongs to someone else (docker/podman/lxc —
#                     measured: the Claude cloud sandbox reports `docker`).
#                     ufw, sysctl, auditd, apparmor, chrony act on THAT kernel.
#   nos_is_wsl        Windows via WSL2 — Ubuntu userland on Microsoft's kernel.
#                     nOS on Windows IS the Linux path (docs/cross-platform.md).
# The uid:gid a NON-ROOT image must run as to use the operator's bind mounts.
# nOS writes configs 0600 / dirs 0700 (SEC-1) owned by the operator; an image
# whose USER is its own uid (Loki/Tempo 10001, Grafana 472, Prometheus nobody)
# then cannot read them on Linux — measured on the cloud lane, 2026-09-26:
# the whole observability stack crash-looped on "permission denied". Docker
# Desktop's file sharing hides this on macOS, so it stays EMPTY there and the
# rendered compose is byte-identical. Templates use it as
#   {% if nos_container_user %}user: "{{ nos_container_user }}"{% endif %}
- name: "[platform] Container uid:gid for bind-mount owners (Linux)"
  ansible.builtin.set_fact:
    nos_container_user: >-
      {{ '' if ansible_os_family == 'Darwin'
         else (ansible_facts['user_uid'] | string) ~ ':' ~ (ansible_facts['user_gid'] | string) }}

- name: "[platform] Host kind (container / WSL)"
  ansible.builtin.set_fact:
    nos_is_container: >-
      {{ ansible_os_family != 'Darwin'
         and ((ansible_facts['virtualization_role'] | default('')) == 'guest')
         and (((ansible_facts['virtualization_type'] | default(''))
               in ['docker', 'podman', 'lxc', 'container', 'containerd', 'openvz', 'systemd-nspawn'])
              or ('container' in (ansible_facts['virtualization_tech_guest'] | default([])))) }}
    nos_is_wsl: "{{ ansible_os_family != 'Darwin' and ('microsoft' in (ansible_facts['kernel'] | default('') | lower)) }}"

- name: "[platform] Compute derived nginx vhost paths + point docker_bin at the platform binary"
  ansible.builtin.set_fact:
    nos_nginx_sites_available: "{{ nos_nginx_etc_dir }}/sites-available"
    nos_nginx_sites_enabled: "{{ nos_nginx_etc_dir }}/sites-enabled"
    # docker_bin defaults to the macOS path (/usr/local/bin/docker). On Linux
    # Docker CE (apt) lands at /usr/bin/docker; without this remap docker-prereqs
    # can't find the binary and blocks on the "waiting for manual fix" pause.
    # On macOS nos_docker_bin == the original docker_bin, so this is a no-op.
    docker_bin: "{{ nos_docker_bin }}"

# ── Traefik's route to a HOST daemon (2026-08-31) ───────────────────────────
# `dynamic/services.yml.j2` routes wing / openclaw / hermes — launchd processes
# on the host's 127.0.0.1 — at `traefik_host_gateway_ip`, whose default is
# `192.168.65.254`. That is Docker DESKTOP's gateway: a macOS address, hardcoded
# as the default for every platform, with a comment inviting a per-host override
# that Linux never had reason to set.
#
# MEASURED in CI 2026-08-31, the first run in which Linux had a Traefik at all:
# every router answered a uniform 500 — face, wing, mailpit and all four Tier-2
# apps — because the upstream IP does not exist on a Linux bridge network. The
# defect is as old as the Linux port; nothing could see it while CI ran with no
# edge proxy, which is fee 39.
#
# `host.docker.internal` rather than a bridge IP: the Traefik compose file
# already maps it (`host.docker.internal:host-gateway`) on BOTH platforms, so
# this resolves without hardcoding a second address that a Docker version could
# move. macOS keeps the literal IP it was measured working with — the comment in
# services.yml.j2 records that the IPv6 `nos-host` alias does NOT reach host
# loopback there, so this is not a place to unify on a guess.
- name: "[platform] Traefik's host-gateway address"
  ansible.builtin.set_fact:
    traefik_host_gateway_ip: >-
      {{ traefik_host_gateway_ip | default('192.168.65.254' if nos_platform == 'macos'
                                           else 'host.docker.internal', true) }}

- name: "[platform] Print platform summary"
  ansible.builtin.debug:
    msg: |
      ── platform ────────────────────────────────────────────────────────
       OS family:        {{ ansible_os_family }}
       Distribution:     {{ ansible_distribution | default('?') }}
       Detected:         {{ nos_platform }}
       Package manager:  {{ nos_pkg_manager }}
       Service manager:  {{ nos_service_manager }}
       Init system:      {{ nos_init }}
       Host kind:        {{ 'container' if nos_is_container else ('wsl2' if nos_is_wsl else 'machine') }}
       Kernel IPv6:      {{ nos_kernel_ipv6 }}
       nginx etc:        {{ nos_nginx_etc_dir }}
       systemd user:     {{ nos_systemd_user_dir }}
      ────────────────────────────────────────────────────────────────────
