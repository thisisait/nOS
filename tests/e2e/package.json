{
  "name": "nos-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "nOS Tier-2 wet-test Playwright suite (Track P scaffolding).",
  "scripts": {
    "test": "playwright test",
    "test:json": "playwright test --reporter=json",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
